# Data properti
tabel_properti = {
'luas_tanah': [70, 70, 70, 100, 100, 100, 120, 120, 150, 150],
'luas_bangunan': [50, 60, 60, 50, 70, 70, 100, 80, 100, 90],
'jarak': [15, 30, 55, 30, 25, 50, 20, 50, 50, 15],
'harga': [500, 400, 300, 700, 1000, 650, 2000, 1200, 1800, 3000]
}
# Fungsi rata-rata data
def hitung_rata_rata(data):
    jumlah = 0
    for item in data:
        jumlah += item
    rata_rata = jumlah/len(data)
    return rata_rata
# Fungsi hitung_standar_deviasi
def hitung_standar_deviasi(data):
    rata_rata_data = hitung_rata_rata(data)
    varians = 0
    for item in data:
        varians += (item - rata_rata_data) ** 2
        varians /= len(data)
    standar_deviasi = varians ** (1/2)
    return standar_deviasi
# Definisikan fungsi untuk menghitung rata-rata dan standar deviasi
# setiap kolom pada tabel_properti yang diberikan oleh key dict.
def deskripsi_properti(tabel):
    for key in tabel.keys():
        print('Rata-rata ' + key + ':')
        print(hitung_rata_rata(tabel[key])) 
        print('Standar deviasi ' + key + ':')
        print(hitung_standar_deviasi(tabel[key]))
        print('')
# Panggil fungsi deskripsi_properti untuk menghitung rata-rata 
# dan standar deviasi setiap kolom pada tabel_properti
deskripsi_properti(tabel_properti)

'''tabel_properti: Ini adalah sebuah dictionary yang berisi data properti. Setiap kunci dalam dictionary ini mewakili jenis data properti (misalnya 'luas_tanah', 'luas_bangunan', 'jarak', 'harga'), dan nilai yang terkait dengan setiap kunci adalah daftar nilai properti tersebut.

hitung_rata_rata(data): Ini adalah fungsi yang menghitung rata-rata dari data yang diberikan. Fungsi ini mengambil satu parameter, yaitu data yang ingin dihitung rata-ratanya. Ini melakukan iterasi melalui setiap elemen dalam data, menambahkannya ke variabel jumlah, dan kemudian membagi jumlah tersebut dengan panjang data untuk mendapatkan rata-rata. Nilai rata-rata kemudian dikembalikan.

hitung_standar_deviasi(data): Ini adalah fungsi yang menghitung standar deviasi dari data yang diberikan. Fungsi ini menggunakan rata-rata dari data yang dihitung dengan fungsi hitung_rata_rata(data). Kemudian, itu menghitung varians dengan mengambil setiap elemen data, mengurangi rata-rata, memangkatkannya, dan kemudian menambahkannya. Setelah mendapatkan total varians, itu dibagi dengan panjang data dan akar kuadrat dari hasilnya diambil untuk mendapatkan standar deviasi. Standar deviasi kemudian dikembalikan.

deskripsi_properti(tabel): Ini adalah fungsi yang mengambil tabel data properti sebagai input dan mencetak rata-rata dan standar deviasi untuk setiap kolom properti. Ini melakukan iterasi melalui setiap kunci dalam tabel (yang merepresentasikan kolom data properti), mencetak judul kolom, memanggil fungsi hitung_rata_rata(data) dan hitung_standar_deviasi(data) untuk mendapatkan rata-rata dan standar deviasi untuk kolom tersebut, dan mencetak nilai-nilai tersebut.

Panggilan fungsi deskripsi_properti(tabel_properti): Ini adalah bagian di mana fungsi deskripsi_properti dipanggil dengan tabel_properti sebagai argumen. Ini akan menghasilkan rata-rata dan standar deviasi untuk setiap kolom dalam tabel_properti dan mencetaknya.

'''